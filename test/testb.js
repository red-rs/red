""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = {
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}







































































































































































let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = {
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { nam
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John"
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"























































































































































































































































































































































































































































































































// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50""
// Variables
let name = "John";
let age = 30;
const PI = 3.14;

// Arrays
let fruits = ["Apple", "Banana", "Orange"];
fruits.push("Mango");
console.log(fruits[0]); // Output: "Apple"

// Hash Maps (Objects)
let person = { name: "John", age: 30, city: "New York" };
console.log(person.name); // Output: "John"

// Class
class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
    calculateArea() {
        return this.width * this.height;
    }
}
Вот смотри разница в потреблении cpu, alacritty vs iterm, 3 раза

let rect = new Rectangle(5, 10);
console.log(rect.calculateArea()); // Output: 50
